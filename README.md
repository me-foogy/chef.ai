# chef.ai
A simple react project which takes the ingredients and gives out the recipe using those ingredients.

The code currently has the hardcoded recipe as I cannot push the frontend code with the API key. Soon I will learn node.js and implement the code in the backend.
